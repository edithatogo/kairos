# Regression Baselines

This directory holds the initial comparison material for Track 31.

- `compare.py` compares current benchmark output against the declared thresholds.
- The benchmark set starts with the preview names recorded in `conductor/performance-thresholds.md`.
