# npm Packaging

Track 09 currently validates the TypeScript package with local npm build, test, and
typecheck gates. Wasm runtime publication remains gated on native Wasm artifact
generation after the Rust Wasm crate moves beyond the dependency-light facade.
