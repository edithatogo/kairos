# R Packaging

Track 07 currently validates the R package surface statically because R/Rscript are
not available on the local PATH. CRAN-style checks remain gated on an R toolchain and
native runtime artifact packaging.
