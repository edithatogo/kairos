# Reproducibility

Every published KairoECS example or benchmark should include:

```text
KairoECS version / commit SHA
host-language package version
scenario manifest
seed manifest
number of replications
hardware/OS/runtime
output schema version
analysis scripts
exact command
```
