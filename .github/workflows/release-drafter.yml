name: Release Drafter

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  draft:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release drafter workflow initialized for governance coverage."
