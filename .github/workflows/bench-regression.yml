name: Benchmark Regression Guard

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  regression-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Validate regression thresholds
        run: python benches/regression/compare.py
