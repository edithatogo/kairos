[package]
name = "kairo-ecs-gpu"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
repository = "https://github.com/edithatogo/kairos"
rust-version = "1.76"

[features]
default = []
gpu = []
wgpu-backend = ["gpu"]
cuda-backend = ["gpu"]

[lints.rust]
unsafe_code = "forbid"
