# Release Team

The release team confirms all release gates pass, verifies versions/changelog, confirms compatibility reports, builds artifacts from the intended commit, generates checksums/SBOMs/attestations, publishes in staged order, and monitors post-release issues.
