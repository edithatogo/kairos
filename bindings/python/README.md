# Python Binding

Track 06 owns this binding.

This slice now has a minimal package surface under `bindings/python/kairo_ecs/` with a matching `pyproject.toml`, import smoke test, and packaging metadata for CI and dry-run builds.

Useful commands:

- `python -m pytest -q`
- `python -m build`
- `python -m pip check`
