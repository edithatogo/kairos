# Language Bindings

Binding implementation roots:

- `python/`
- `r/`
- `julia/`
- `typescript/`
- `csharp/`
- `go/`

Keep package manifests out of a binding directory until its track is ready for CI to enforce real smoke tests.

