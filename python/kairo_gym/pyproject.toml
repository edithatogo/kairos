[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "kairo-gym"
version = "0.1.0"
description = "Gymnasium-compatible reinforcement-learning wrapper for KairoECS"
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
gymnasium = ["gymnasium>=0.29.1"]
