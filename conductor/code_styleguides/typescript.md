# TypeScript/Wasm Style Guide

- Use strong TypeScript types and generated `.d.ts` files.
- Test Node and browser paths separately.
- Use `vitest` for unit tests and `playwright` for browser smoke tests.
- Avoid undocumented copies between Wasm memory and JS objects.
- Use Apache Arrow JS for telemetry examples and event log viewers.
