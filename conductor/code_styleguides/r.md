# R Style Guide

- Prefer stable C ABI wrappers and external pointers.
- Use `roxygen2`, `testthat`, `pkgdown`, `lintr`, and `styler`.
- Start with R-universe/GitHub release distribution before CRAN.
- Use Arrow package examples for telemetry and data frames.
- Clearly document object ownership and `finalize` behavior.
