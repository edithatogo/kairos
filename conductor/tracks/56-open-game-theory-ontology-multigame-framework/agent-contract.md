# Track 56 Agent Contract

## interop-agent

Owns ontology semantics, Turtle/JSON-LD interpretation, external schema
provenance, and standards-facing claim boundaries.

## ecs-agent

Owns dense component storage behavior, `EntityId` relationship components, and
proof that graph topology remains data-oriented.

## core-scheduler-agent

Owns execution integration, solver scheduling boundaries, and default-build
feature isolation in the Kairos engine.

## model-zoo-agent

Owns game-theory examples, fixture maturity labels, and model-zoo integration
after solver and ontology gates pass.

## api-governance-agent

Owns protected public API review for generated component structs, solver
facades, and any future binding exposure.
