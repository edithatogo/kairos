# Track 54 Agent Contract

## cloud-agent

Owns container, Kubernetes, Slurm, provider canary, and runtime evidence
collection.

## distributed-agent

Reviews MPI launch and distributed runtime requirements.

## gpu-compute-agent

Reviews GPU scheduler and runtime requirements.

## release-agent

Reviews protected publication and release-manager approval boundaries.
