# Track 57 Agent Contract

- Use structured RDF/JSON parsing where available; avoid ad hoc parsing beyond narrow fixture validation helpers.
- Preserve source provenance in the canonical IR.
- Keep parser errors typed and testable.
- Commit after every task and leave local gates passing.
