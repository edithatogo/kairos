# Track 60 Agent Contract

- Solvers must operate on ECS component data, not detached object graphs.
- Deterministic ordering is required for every solver output.
- Numeric tolerance and tie-breaking rules must be documented and tested.
- Commit after every task and record review/push/Actions evidence after every phase.
