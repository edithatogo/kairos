# Risk Register: Track 29 Wave Manager & Execution Gatekeeper

Current validation date: 2026-05-06.

| Risk | L | I | Sev | Mitigation | Owner | Current evidence | Escalation trigger |
|---|---:|---:|---:|---|---|---|---|
| Track statuses are ahead of their dependency evidence | 5 | 5 | 25 | Keep the new gate blocking until dependencies are marked `Done` or an ADR documents a maintainer override. | wave-manager-agent | Blocking validation reports 89 direct dependency blockers and 181 transitive dependency blockers. | Any release-readiness claim while these blockers remain. |
| Previous fixed 0-5 wave assumption hides the actual dependency depth | 4 | 4 | 16 | Derive waves from `conductor/tracks.yaml`; document current Waves 0-6. | wave-manager-agent | Report-only validation assigns Track 39 to Wave 6 because it depends on Wave-5 Track 15. | Any document or gate that assumes a fixed maximum wave. |
| Wave policy is too rigid and blocks legitimate parallel planning work | 3 | 4 | 12 | Gate only advancement statuses (`In Progress`, `In Review`, `Done`) and require ADR-based override for intentional exceptions. | governance-agent | Gate semantics are documented in `conductor/wave-policy.md` and gate YAML. | Two consecutive overrides for the same dependency pair. |
| A track dependency is misconfigured in `tracks.yaml` | 3 | 4 | 12 | Validate every dependency target and fail cycles in `validate-wave-gates.ps1`; keep `scripts\validate_conductor_dag.ps1` as supporting validation. | foundation-agent | DAG validator passed with 41 tracks, 47 agents, 0 errors, 0 warnings. | Unknown dependency or dependency-cycle issue reported. |
| A track is marked `Done` but its outputs are incomplete | 3 | 4 | 12 | Track-specific `Done` criteria remain with each track; Track 29 checks required conductor artifacts and dependency status only. | track owners | Artifact validator passed with 41 track directories and no missing artifacts. | Downstream track blocked by incomplete upstream output despite upstream `Done`. |
| Critical-path heatmap becomes stale after `tracks.yaml` changes | 3 | 3 | 9 | Regenerate heatmap with `validate-wave-gates.ps1 -ReportOnly` whenever dependency or status data changes. | wave-manager-agent | Heatmap is generated by the validator; top current blockers are 00, 01, 12, 26, and 04. | Heatmap timestamp predates a dependency/status change. |
| Maintainers override wave gates too frequently, eroding policy trust | 2 | 4 | 8 | Require an ADR naming the failed gate, dependency chain, compensating control, approver, and expiry. | governance-agent | Exception path is documented in `conductor/wave-policy.md`. | More than 3 overrides in a release cycle. |
